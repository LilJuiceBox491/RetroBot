/* eslint-disable no-unused-vars */
// Remove the above comment.

module.exports = {
    config: {
        name: '' // The event name.
    },
    run: (client) => {
        // Code here
    }
}

// You can remove these comments:
// The function parameters for run() must start with client, and anything else the event emits.
// Example, if your event is Client#guildCreate, your function parameters would be: client, guild
// If your event is Client#message, your function parameters would be: client, message
// And so on...